# Azure Hello World Service

This is just an extremely simple Hello World service that sends out a JSON document that contains the contents of the AZURE_HELLO_VERSION environment variable (or if unset, 0.1).

It is mostly intended for testing of various Azure hosting options for containers.

